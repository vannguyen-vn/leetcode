const solution = require('./firstBadVersion');

describe('The first bad version', () => {
  it(`Should return the function`, () => {
    let isBadVersion = true;
    expect(typeof solution).toBe('function');
  });
});